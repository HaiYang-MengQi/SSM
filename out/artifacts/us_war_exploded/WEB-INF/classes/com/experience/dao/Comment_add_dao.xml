<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.experience.dao.Comment_add_dao">
    <insert id="addComment" parameterType="com.experience.entity.Comment">
    insert into comment(id,comment,time) values (#{id},#{comment},#{time});
</insert>
</mapper>